const range = (n, end) => {
  //TODO: 生成一个左开右闭的数组,省略第一个参数时默认从1开始
  // 考察点 参数判断

  return [n, end]
}

const iterSum = (n) => {
  //TODO:使用for in 求和 模拟Python的 for in
  return n
}

export { range, iterSum }
