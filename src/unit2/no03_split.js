//获取藏头诗隐藏的单词
export const getHeadWord = (poetryString) => {
  return poetryString
}

//获取时间显示字符串
export const getTimeString = (numberTimeStr) => {
  return numberTimeStr
}

//屏蔽不雅词汇fuck
export const shieldFuck = (string) => {
  return string
}
